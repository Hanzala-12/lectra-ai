import { LecturePicker } from '../components/LecturePicker';

export function Quiz() {
  return (
    <LecturePicker
      title="Quiz & Assessment"
      subtitle="Pick a lecture to generate and take a quiz."
      tab="quiz"
    />
  );
}
