# System Dependencies
setuptools==68.0.0

# Core Dependencies
torch>=2.0.0,<2.6.0
torchaudio>=2.0.0,<2.6.0
numpy>=1.24.0,<2.0.0
scipy>=1.10.0,<1.15.0

# Audio Processing
librosa>=0.10.0,<0.11.0
soundfile>=0.12.1,<0.13.0
pydub>=0.25.1,<0.26.0

# DeepFilterNet for noise reduction
deepfilternet>=0.5.6,<0.6.0

# Pre-processing
nara-wpe>=0.0.11,<0.1.0
pyrnnoise>=0.4.3,<0.5.0

# Post-diarization spectral noise gating
noisereduce>=3.0.0,<4.0.0

# Voice Activity Detection
webrtcvad-wheels>=2.0.10,<3.0.0  # Pre-compiled version for Windows compatibility
silero-vad>=5.1.0,<7.0.0

# Diarization - tightly constrained to avoid resolution issues
pyannote.audio>=3.1.0,<3.2.0
# Neural speech enhancement — MetricGAN+ final denoise stage (also diarization backbone)
speechbrain>=1.0.0,<1.1.0
lightning>=2.4.0,<2.4.1
torchmetrics>=1.6.1,<1.7.0
lightning-utilities>=0.10.0,<0.12.0

# PyTorch ecosystem - constrain to avoid conflicts
fsspec>=2022.5.0,<2025.0
aiohttp>=3.9.0,<3.11.0

# Speech Recognition
faster-whisper>=1.0.0,<2.0.0

# Study assistant — NLP/LLM + RAG (notes, quiz, schedule, evaluation, chat)
# LLM calls go over HTTP (httpx, already pulled in by FastAPI).
scikit-learn>=1.3.0,<1.6.0
# RAG retrieval — sentence embeddings (all-MiniLM-L6-v2, ~90MB, fully offline
# once cached) reusing the torch already installed above for the audio
# pipeline. rag_engine.py falls back to the scikit-learn TF-IDF above
# automatically if this isn't installed or the model can't load.
sentence-transformers>=6.0.0,<7.0.0

# Audio recap — narrates a spoken-style summary (tts_engine.py). Reuses the
# onnxruntime already installed above. The actual voice model is NOT pip
# packaged — download it once with:
#   python -m piper.download_voices --download-dir models/piper en_US-lessac-medium
piper-tts>=1.7.0,<2.0.0

# Video Processing
moviepy>=1.0.3,<2.0.0
ffmpeg-python>=0.2.0,<0.3.0

# Utilities
tqdm>=4.65.0,<5.0.0
pyyaml>=6.0,<7.0

# Web Interface
streamlit>=1.31.0,<2.0.0

# Backend API
fastapi>=0.109.0,<0.120.0
uvicorn[standard]>=0.27.0,<0.32.0
python-multipart>=0.0.6,<0.1.0
websockets>=12.0,<14.0

# Study assistant — uploaded PDF reference files for the RAG chat
pypdf>=4.0.0,<7.0.0
